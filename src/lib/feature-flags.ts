// Feature flags — addendum v2 §5.5.
export const FEATURE_FLAGS = {
  // Polvo de estrellas en el cursor. Default: false (rompe la atmósfera
  // editorial). Solo activar si el cliente lo confirma explícitamente.
  cursorStarDust: false,
} as const;
